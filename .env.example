# Authentication Secrets
# Generate new secrets with: openssl rand -base64 32
AUTH_SECRET=
NEXTAUTH_SECRET=

# API Configuration
# Use NEXT_PUBLIC_API_HOST for all frontend API calls and NextAuth token refresh
# Example: http://localhost:3000
NEXT_PUBLIC_API_HOST=

# Deprecated: use NEXT_PUBLIC_API_HOST instead
# Keeping for compatibility in commented code paths only
NEXT_PUBLIC_NEXTAUTH_API_HOST=

# WebSocket Configuration
NEXT_PUBLIC_SOCKET_URL=

# Optional: Environment
NODE_ENV=development
